# 更新记录

中文 | [English](CHANGELOG.md)

## 0.5.1（实验版）

- 完整模式（服务账号或访问令牌）下的 Priority 改走 OpenAI 兼容接口，与 Standard 一致，不再因原生接口不支持的字段返回 `unsupported_native_fields`；“流式”模式仍用原生逐步输出，Express 和 Flex 仍只走原生接口。
- 从兼容接口报告档位的位置 `usage.extra_properties.google.traffic_type` 读取实际档位，Priority 日志显示 `ON_DEMAND_PRIORITY`，不再显示“上游未报告”。
- 通过 84 项本地测试、57 个发布文件检查和 2 次有上限的真实 Priority 请求，详见[验收说明](docs/VALIDATION.md)。

## 0.5.0（实验版）

- 提示词提交失败重试的触发规则可自定义：控制台每行一条，命令行用 `GEMINI_PROMPT_RETRY_MATCHES` 以 `|` 分隔。规则为不区分大小写的包含匹配，默认只有 “The prompt could not be submitted”。现在还会检查 OpenAI 兼容接口的拒答 `refusal`（Vertex 兼容接口的流式回复就把这类拦截放在这里）和原生接口的 `promptFeedback` 拦截原因；仍不检查模型生成的正文。
- 发送前去掉 Google 文档明确写明不支持的字段，例如 Gemini 3.7/3.8 Flash 的惩罚参数、候选数和采样参数，不再先失败一次。响应头 `x-gemini-dropped-params` 和日志 `droppedParams` 列出去掉的字段名。
- `UPSTREAM_TIMEOUT_MS` 现在同时限制等待响应头和正文分段的时间。此前 Node 内置 fetch 无论设置多少，都在 300 秒时放弃。
- 抗截断还原失败时报告具体错误码，不再显示笼统的协议错误。
- 通过 83 项本地测试、57 个发布文件检查和 1 次有上限的真实流式请求，详见[验收说明](docs/VALIDATION.md)。

## 0.4.1（实验版）

- 区分消息缺失、消息类型错误、意外流式片段与候选项类型错误；即使校验失败，也保留失败候选项已知的结束原因。
- 修复非流式及缓冲模式抛出错误时丢失诊断信息的问题。空回复保留其分类，客户端错误与 GUI 日志携带同一份固定诊断字段，不记录私人内容。
- 验证开启文本重试后，未命中的回包保持逐字节一致，包括分段 Unicode 与超过检查上限的回包。此类错误不会触发文本插入或额外提交。
- 通过 77 项本地测试及 56 个发布文件检查。本次修复诊断信息，无法恢复上游没有返回的正文。

## 0.4.0（实验版）

- 新增三个独立开关：隐藏无可用路由模型、Gemini 3.7/3.8 Flash 文本预填充转 USER、提示词提交失败后重试一次。前两项默认开启，重试默认关闭且需要自定义文本。
- 模型可以停用并保留配置；停用版本与已知上游 401 鉴权失败的连接可从客户端模型列表隐藏，临时错误不影响显示。
- 在正文输出前识别 HTTP、200 JSON 及 SSE 开头的指定提交错误；使用相同模型、凭据和档位重试，将文本插入开头的 system/developer 消息之后。输出开始后不重试，第二次失败不循环。
- GUI 新增持久化文本编辑、粗略 token/UTF-8 计数、输入校验、转换/重试日志标记和测试费用提示。日志不记录自定义文本和凭据。
- 通过 74 项本地测试与隔离 GUI 的保存/重载、模型隐藏、重试日志、深浅主题及响应式检查。本次未调用收费推理。范围见[验收说明](docs/VALIDATION.md)。

## 0.3.1（实验版）

- 校验普通回复、真实工具与抗截断还原结果，包括流式各候选的结束原因和 DONE。空回复、错误工具参数、上游错误事件及中断不再算作成功，也不自动重放；长度限制和拒绝保持可见。
- 改用原生 JSON Schema 字段，保留封闭对象、属性名、可空类型、本地引用及未限定元素的数组。不支持的约束在本地返回具体参数路径，完成的原生结构化输出按支持的 Schema 校验，不自动修补正文。
- 日志和界面增加响应完整性状态，以请求编号关联诊断；日志仍不包含提示词、回复、工具参数和凭据。
- 通过 53 项本地测试，并完成三个小额度 Gemini 3.8 真实请求：Standard 普通、渐进抗截断和 Flex 严格 Schema。范围和限制见[验收说明](docs/VALIDATION.md)。

## 0.3.0（实验版）

- 使用当前鉴权方式分页拉取、搜索 Google Gemini 发布目录；提示权限失败，并支持手动填写模型 ID。
- 最多保存 100 个独立模型版本，包括同一上游的正常、非流式抗截断和流式抗截断；升级保留已有名称与行为。
- `/v1/models` 返回已保存版本，按名称路由并在 JSON/SSE 中返回别名。非流式抗截断始终等待上游完整响应，客户端要求 SSE 时再一次性交付正文。
- 总览与连接测试可以选择模型版本，请求日志增加模型、模式和上游 ID。
- 目录上游鉴权失败不再误触发本地退出；正常版本也会识别 SSE 中断和上游错误。
- 本地模拟覆盖目录分页与错误、迁移持久化及各模式协议。本次更新未发送真实推理请求。

## 0.2.0（实验版）

- 新增本地 GUI：总览、连接配置、请求日志与有界测试，支持深浅主题和窄屏。
- 支持项目 ID、完整服务账号 JSON 粘贴/导入、Express API Key，以及原有 OAuth Token。
- 新增 Standard / Flex / Priority 选择，原生接口覆盖普通和流式请求，日志区分请求档位与实际档位。
- 配置在仓库外持久化，支持版本冲突检测、凭据不回显和不中断已有回复的热应用。
- `npm start` 默认启动控制台；`npm run gateway` 保留仅命令行模式，Windows 可双击 `Start-GUI.cmd`。
- 新增本地协议、安全边界与持久化测试。GUI 和档位组合使用本地模拟上游验收，未发送真实收费请求。

## 0.1.0（实验版）

首个独立版本，从本地 Node.js router 提取 Gemini 3.7 Flash 工具调用抗截断传输。

- 符合条件的流式请求使用 Vertex 原生 `partialArgs`，在生成过程中发送普通 OpenAI SSE 正文。
- 如果翻译会丢失字段，保留兼容接口回退。已有客户端工具、结构化输出等冲突会跳过包装。
- 请求日志记录还原结果、结束原因和流完成状态，并关联响应中的请求 ID。
- 包含启动说明、SillyTavern 设置、本地协议测试和中英文文档。
- 标注方案来源 [Xeltra233 / Antigravity-gateway](https://github.com/Xeltra233/Antigravity-gateway)，保留其 MIT 许可证。

默认检查使用模拟上游响应。已完成和未完成的测试范围见[验收说明](docs/VALIDATION.md)。
